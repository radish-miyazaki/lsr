[package]
name = "lsr"
version = "0.1.0"
edition = "2021"

[dependencies]
clap = { version = "4", features = ["derive"] }
chrono = "0.4"
tabular = "0.2"
users = "0.11"

[dev-dependencies]
assert_cmd = "2"
predicates = "3"
rand = "0.8"
